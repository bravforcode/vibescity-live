# คู่มือการติดตั้งและใช้งาน TestSprite MCP (ภาษาไทย)

เอกสารนี้จะแนะนำวิธีการติดตั้งและกำหนดค่า **TestSprite MCP Server** เพื่อเพิ่มความสามารถในการทดสอบซอฟต์แวร์ให้กับ AI Assistant ของคุณ

## 1. สิ่งที่ต้องเตรียม (Prerequisites)

ก่อนเริ่มใช้งาน กรุณาตรวจสอบว่าเครื่องของคุณได้ติดตั้ง **Node.js** แล้ว
สามารถตรวจสอบได้โดยเปิด Terminal (PowerShell หรือ CMD) และพิมพ์คำสั่ง:

```bash
node -v
npm -v
```

หากยังไม่ได้ติดตั้ง สามารถดาวน์โหลดได้ที่ [nodejs.org](https://nodejs.org/)

## 2. การตั้งค่า Configuration

คุณต้องเพิ่มการตั้งค่าลงในไฟล์ Configuration ของ MCP Client ที่คุณใช้งาน (เช่น Claude Desktop หรือ IDE Extension)

### ตำแหน่งไฟล์ Config
*   **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`
*   **Mac/Linux**: `~/Library/Application Support/Claude/claude_desktop_config.json`

หากหาไฟล์ไม่เจอ สามารถสร้างไฟล์นี้ขึ้นมาใหม่ในตำแหน่งดังกล่าว

### โค้ดที่ต้องเพิ่ม (JSON)

ให้คัดลอกโค้ดด้านล่างนี้ไปใส่ในส่วน `mcpServers` ของไฟล์ config:

```json
{
  "mcpServers": {
    "TestSprite": {
      "command": "npx",
      "args": [
        "-y",
        "@testsprite/testsprite-mcp@latest"
      ],
      "env": {
        "API_KEY": "your-testsprite-api-key"
      }
    }
  }
}
```

> **หมายเหตุ:** เราได้บันทึก API Key ของคุณไว้ในไฟล์ `.env` ของโปรเจคแล้วในชื่อ `TESTSPRITE_API_KEY` เพื่อความปลอดภัยและการอ้างอิง แต่สำหรับการตั้งค่า MCP แนะนำให้ใส่ Key ลงในไฟล์ config โดยตรงเพื่อให้แน่ใจว่าทำงานได้ทันที

## 3. วิธีการใช้งาน & ความสามารถ

เมื่อติดตั้งเสร็จแล้ว ให้ **Restart** โปรแกรม AI Assistant ของคุณ (เช่น ปิดและเปิด Claude Desktop ใหม่)

หลังจากนั้น AI จะมีความสามารถเพิ่มขึ้นดังนี้:

*   **สร้าง Test Case อัตโนมัติ**: สั่งให้ AI เขียนเทสสำหรับฟีเจอร์ต่างๆ
*   **รัน Test**: ให้ AI ช่วยรันชุดทดสอบ (ถ้ามีการ integrate กับระบบ CI/CD)
*   **วิเคราะห์ผลเทส**: ส่งผลลัพธ์การเทสให้ AI ช่วยวิเคราะห์หาสาเหตุของ Bug

### ตัวอย่างคำสั่งที่ใช้ได้ (Prompting)
*   "ช่วยเขียน Unit Test สำหรับไฟล์ `Login.vue` ให้หน่อย โดยใช้ TestSprite"
*   "สร้าง Test Scenarios สำหรับระบบตะกร้าสินค้า"
*   "ตรวจสอบความครอบคลุมของการทดสอบ (Test Coverage)"

ขอให้สนุกกับการทดสอบระบบที่มีประสิทธิภาพมากขึ้น! 🚀
