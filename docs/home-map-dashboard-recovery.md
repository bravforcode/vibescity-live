# Home / Map / Dashboard Recovery

## Scope

- Normalize English-first locale behavior across routing, stored preference, and SEO tags.
- Replace the Partner Program hard redirect with a page-level gated access flow.
- Stabilize the MapLibre advanced path so shader/image failures do not collapse the map shell.
- Align popup/modal media hierarchy with the approved live-bar-first layout.

## Acceptance Highlights

- First visit resolves to English without redirect loops.
- `/partner` opens a gated page for non-members instead of bouncing home.
- Map stays usable when advanced effects fail.
- Venue popup and modal show media or an intentional fallback, with the live bar inside the card.
