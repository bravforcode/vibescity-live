[pytest]
asyncio_mode = auto
testpaths =
    backend/tests
norecursedirs =
    .git
    .venv
    .vercel
    .vercel_python_packages
    node_modules
    dist
    build
    output
    tmp
    .agent
    .agents
